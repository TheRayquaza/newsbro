# https://fluxcd.io/flux/components/source/helmrepositories/
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: helm-repo
  namespace: cert-manager
spec:
  interval: 10m0s
  type: default
  url: https://charts.jetstack.io # jetstack repo
  provider: generic
