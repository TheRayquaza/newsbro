---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: repo-account-secrets
  namespace: apps
spec:
  refreshInterval: 10m
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: repo-account-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          reconcile.fluxcd.io/watch: Enabled
  data:
    - secretKey: DATABASE_USERNAME
      remoteRef:
        key: repo-account/postgres
        property: username
    - secretKey: DATABASE_PASSWORD
      remoteRef:
        key: repo-account/postgres
        property: password
