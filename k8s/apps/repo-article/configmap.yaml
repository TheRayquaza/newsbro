apiVersion: v1
kind: ConfigMap
metadata:
  name: repo-article-config
  namespace: apps
data:
  # Application
  GIN_MODE: release
  PORT: "8080"
  ENVIRONMENT: prod
  # Postgres
  DATABASE_NAME: repo-article
  # Kafka
  KAFKA_GROUP_ID: repo-article-group
